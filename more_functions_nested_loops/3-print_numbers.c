#include "main.h"
/**
 * print_numbers - prints the numbers, from 0 to 9, followed by a new line.
 *
*/
void print_numbers(void)
{
	for (a = '0'; a <= 9; a++)
	{
		_putchar (a);
		continue;
	}
	_putchar ('\n');
}
