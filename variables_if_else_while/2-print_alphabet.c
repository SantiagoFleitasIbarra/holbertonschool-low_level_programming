#include <stdio.h>

/**
 * main - Prints Write a program that prints the alphabet in lowercase, followe
 * d by a new line.
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	char alpha;

	for (alpha = 'a'; alphabet <= 'z' ; alpha++)
	{
	putchar(alpha);
	}

	putchar ('\n');
	return (0);
}
